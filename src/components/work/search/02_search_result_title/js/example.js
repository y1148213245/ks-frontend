/**
 * 搜索结果标题
 */

import component from "../search_result_title.vue";


const name = component.name;  // 组件标签名

const title = "标题组件";  // 组件title

const description = `搜索结果标题`;  // 组件描述信息

const dev = {
};

const prod = {
};

export {name, title, dev, prod, description};
