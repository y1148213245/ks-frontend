/**
 * 热门搜索
 */

import component from "../hotWords.vue";


const name = component.name;  // 组件标签名

const title = "热门搜索组件";  // 组件title

const description = `热门搜索关键词`;  // 组件描述信息

const dev = {
  
};

const prod = {
  
};

export {name, title, dev, prod, description};
