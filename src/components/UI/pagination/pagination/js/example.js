/**
 * created by song 2018/1/9
 * 分页组件
 */

import component from "../pagination.vue";


const name = component.name;  // 组件标签名

const title = "分页组件";  // 组件title

const description = `分页`;  // 组件描述信息

const dev = {
  
};

const prod = {
  
};

export {name, title, dev, prod, description};
