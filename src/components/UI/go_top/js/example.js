/**
 * created by song 2018/1/9
 * 右下角二维码和回到顶部按钮
 */

import component from "../goTop.vue";

const name = component.name; 

const title = "回到顶部组件";

const description = `右下角二维码和回到顶部按钮`;

const dev = {
  
};

const prod = {
  
};

export {name, title, dev, prod, description};
